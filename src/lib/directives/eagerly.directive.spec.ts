import { EagerlyDirective } from './eagerly.directive';

describe('EagerlyDirective', () => {
  it('should create an instance', () => {
    const directive = new EagerlyDirective();
    expect(directive).toBeTruthy();
  });
});
