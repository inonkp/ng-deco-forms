import { TargetGroupPipe } from './target-group.pipe';

describe('TargetGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new TargetGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
